import React from 'react'
import { StaticImage } from 'gatsby-plugin-image'

const Hero = () => {
  return (
    <header className="hero">
      <StaticImage />
    </header>
  )
}

export default Hero
